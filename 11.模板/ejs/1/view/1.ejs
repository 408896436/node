<!doctype html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Document</title>
</head>
<body>
	<div><%= name %></div>
	<ul>
		<% for(var i=0; i<arr.length; i++) {%>
			<li> <%= arr[i] %> </li>
		<% } %>
	</ul>
	<ol>
		<% for(var i in json) {%>
			<li> key : <%= i %> , value : <%= json[i] %> </li>
		<% } %>
	</ol>
</body>
</html>